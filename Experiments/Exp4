// Palindrome Checker
// Name: Sohal Nakhate PRN: 24070521199
// Scope, closure, error handling using try-catch

// Create anonymous Function without a Name
let welcome = function() {
    console.log("Palindrome Checker Program.");
};

welcome();

// Global Variable
let language = "JavaScript";

function scopeDemo() {
    // Local Variabl
    let topic = "Functions";
    console.log("Language:", language);
    console.log("Topic:", topic);
}

scopeDemo();

// Closure Example
function createCounter() {
    let count = 0;
    return function() {
        count++;
        console.log("Counter:", count);
    };
}

const increment = createCounter();
increment(); // Counter: 1
increment(); // Counter: 2

// Palindrome Checker using Try-Catch and Arrow Function
const checkPalindrome = (str) => {
    try {
        if (typeof str !== "string") {
            throw new Error("Input must be a string");
        }
        
        let reversed = str.split("").reverse().join("");
        if (str === reversed) {
            console.log(`${str} is a Palindrome.`);
        } else {
            console.log(`${str} is not a Palindrome.`);
        }
    } catch (error) {
        console.log("Error:", error.message);
    }
};

checkPalindrome("madam");
